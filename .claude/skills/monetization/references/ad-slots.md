# 広告/アフィリ枠の配置

- 枠は**寸法を固定**して予約（min-height 指定）→ あとから実広告が入っても CLS=0。
- 推奨位置：結果カードの下、本文中盤（H2区切り）、フッター前。ファーストビュー占有や過剰挿入は避ける。
- アフィリは文脈に沿うもの（そのツールの対象に直結する商品/口座/サービス）。
- 実広告コード（AdSense）は env `NEXT_PUBLIC_ADSENSE_CLIENT` がある時だけ注入。無い時は placeholder。
